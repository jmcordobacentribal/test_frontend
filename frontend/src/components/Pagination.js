import React from 'react'

const Pagination = ({ list, currentPage, nextPage, totalItemsPerPage, handlePageChange }) => {

  const totalPages = Math.ceil(list.length / totalItemsPerPage)


  return ( <></>)
}

export default Pagination